class Solution:
    def reverse(self, x: int) -> int:
        count = 0
        num=[]
        x_inverse = 0

        if pow(-2,31) <= x <= pow(2,31) - 1:
            while x%10!=0:
                num.append(x%10)
                count=count+1  
                x=x/10
                
            for k in num:
                x_inverse+=k*pow(10,count) //Problem 
                count = count-1
         
        return x_inverse/10
       

//Problem
OverflowError: int too large to convert to float
    x_inverse+=k*pow(10,count)
Line 14 in reverse (Solution.py)
    ret = Solution().reverse(param_1)
Line 38 in _driver (Solution.py)
    _driver()
Line 49 in <module> (Solution.py)